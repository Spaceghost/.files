# Artwork by theme

Each named theme receives its own directory, such as `gruvbox-dark/` or
`spaceghost/`. Generate with `mbp-intel-wallpaper generate --theme <theme-id>`.
Descriptors live in `alpine/themes/`. PNG/JSON pairs are discovered recursively;
the gallery preserves all older flat artwork and offers every collection.
