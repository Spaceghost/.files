# Sun and moon evidence — 2026-09-08

`masthead-headless.png` is the live generated masthead card configuration
(`~/.local/state/oldbook/conky/masthead.conf`, produced by `oldbook-conky plan`
after the panels.json change) rendered by Conky inside a private headless
SwayFX session: day, date, the Ghost Planet line, then the new sunrise /
sunset / moon line from `oldbook-astro panel`. `astro-status.json` is the
`oldbook-astro status` output at the time, computed offline from the local
`~/.config/oldbook/location.json` (Los Angeles, not committed).

The live desktop was refitted with `oldbook-conky layout`; all six cards
restarted and the masthead config carries the new line. The physical card was
not inspected by eye in this session (display off and locked at the time), and
a full night of nocturne-weighted rotation plus the wlsunset ramp after sunset
remain to be observed.
