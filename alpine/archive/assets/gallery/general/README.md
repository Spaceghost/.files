# Unthemed artwork

`oldbook-wallpaper generate --theme none` saves a new PNG and matching JSON here.
The scene chooses its own colors. Keep the sidecar beside its image; it records
the prompt, image hash and generation metadata for restoration through Fossil.
