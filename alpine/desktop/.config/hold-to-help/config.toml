# Oldbook keeps the established Super hold; physical Caps remains mapped to Escape.
# Select "capslock" here if that is your preferred hold trigger.
trigger = "super"
hold_seconds = 0.5
backend = "auto"
