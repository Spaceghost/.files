# Screenshot annotation in the Gruvbox palette. oldbook-screenshot opens each
# capture here after the flash and shutter; Enter copies and closes, Escape
# closes, Ctrl+S overwrites the capture with the annotated version.
[general]
fullscreen = false
early-exit = true
corner-roundness = 12
initial-tool = "arrow"
copy-command = "wl-copy"
annotation-size-factor = 1.0
save-after-copy = false
default-hide-toolbars = false
disable-notifications = true
actions-on-enter = ["save-to-clipboard", "exit"]
actions-on-escape = ["exit"]
actions-on-right-click = ["save-to-clipboard"]

[font]
family = "Inter"
style = "Bold"

[color-palette]
palette = ["#fabd2f", "#fb4934", "#b8bb26", "#83a598", "#d3869b", "#fe8019"]
custom = ["#ebdbb2", "#282828", "#8ec07c", "#d79921", "#cc241d", "#458588"]
