# Keep the upstream speaker mixer controls and volume mapping.
.include /usr/share/alsa-card-profile/mixer/paths/analog-output-speaker.conf

# A false positive must not make the built-in speakers unavailable.
[Jack Headphone]
state.plugged = unknown
state.unplugged = unknown
