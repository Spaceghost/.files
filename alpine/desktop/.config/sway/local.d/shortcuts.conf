# One process per Sway socket; reloads safely leave the existing service running.
# Start the full guide directly: oldbook-shortcuts delegates to the portable
# rewrite, which has neither the search box nor the overlay that stays open
# after Super is released.
exec_always --no-startup-id ~/.local/bin/superhold daemon
