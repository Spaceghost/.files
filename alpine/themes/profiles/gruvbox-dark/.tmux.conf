# Preserve the historic C-a workflow when it has no plugin manager to bootstrap.
# A local overlay must never fetch TPM or any other runtime dependency.
if-shell 'test -f "$HOME/.files/.tmux.conf" && ! grep -Eq "tmux-plugins|/tpm" "$HOME/.files/.tmux.conf"' 'source-file "$HOME/.files/.tmux.conf"'

set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -g status-position top
set -g status-style "bg=#1d2021,fg=#ebdbb2"
set -g message-style "bg=#504945,fg=#fbf1c7"
set -g pane-border-style "fg=#504945"
set -g pane-active-border-style "fg=#fabd2f"
set -g status-left "#[fg=#fabd2f,bold] 󰆍 #S #[default]"
set -g status-right "#[fg=#8ec07c]#(whoami)@#H #[fg=#a89984]%a %d %b %H:%M "
set -g status-left-length 32
set -g status-right-length 60
setw -g window-status-format " #[fg=#928374]#I:#W "
setw -g window-status-current-format "#[bg=#504945,fg=#fbf1c7,bold] #I:#W #[default]"
set -g clock-mode-colour "#fabd2f"
set -g mode-style "bg=#665c54,fg=#fbf1c7"

# Forward the active pane title to Sway and the workspace decoration.
set -g set-titles on
set -g set-titles-string "#{pane_title}"
