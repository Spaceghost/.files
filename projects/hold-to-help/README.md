# Compatibility path

This directory preserves the earlier source command and Python import path
under the old name. Nothing is developed here.

The guide that actually runs is [`../superhold-guide/`](../superhold-guide/).
The portable Qt project that inherited this project's name directly is
[`../superhold/`](../superhold/), which does not run here either. Earlier
revisions of this file sent readers to that one for new development, which is
how a change lands in a tree the keyboard never reaches; see
[`../README.md`](../README.md).
