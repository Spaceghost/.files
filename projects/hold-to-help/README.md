# Compatibility path

The standalone project is now [Superhold](../superhold/README.md).
This directory preserves the earlier source command and Python import path;
new development, builds and releases use `projects/superhold`.
